# Optional: API key for LLM-enhanced extraction (leave empty for rule-based only)
# OPENAI_API_KEY=

# Server
HOST=0.0.0.0
PORT=8000

# Routing threshold (currency in same unit as estimates)
FAST_TRACK_DAMAGE_THRESHOLD=25000

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
